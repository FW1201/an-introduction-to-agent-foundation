# Wiki 規則

- `raw/`：只放公開、授權或去識別的原始素材。
- `wiki/`：放 Agent 可查詢且可追溯的 Markdown 知識頁。
- `outputs/`：放正式成果；草稿應標示 `draft`。
- `logs/`：放不含敏感資料的操作紀錄。
- `wiki/log.md` 為 append-only 日誌。

每個案例建立 `wiki/projects/<project-id>/`，並使用 brief、sources、knowledge、decisions、qa、outputs、reflection 七份文件。
