# Skills 安裝工作流

1. 先依角色、案例與 `catalog/skills.json` 推薦一個最小 Skill。
2. 說明該 Skill 的工作範圍、來源、前置工具、全域／專案 scope 與驗證方式。
3. 使用者確認後才安裝，並記錄固定版本或 commit。
4. 重新載入 Agent，執行最小驗證案例。
5. 將結果寫入 install log 與 Wiki；失敗時提供回復方式。

Claude Code 使用者可選裝 Anthropic `document-skills`。它只處理文件格式工作，不取代教育 Skill 的內容判斷。
